let name = "hitesh"
let age = 18
let isLoggedIn = false

// primitive datatype

// null(standalone value(representation of null value)) null is object
// undefined (didnt given value)
// symbol

// non primitive datatype
//

console.log(typeof(null));//type is also obj
console.log(typeof(undefined));